# Core FastAPI and web framework dependencies (matching your venv)
fastapi==0.118.0
uvicorn==0.37.0
pydantic==2.11.10
pydantic-core==2.33.2
starlette==0.48.0

# Database and data handling
SQLAlchemy==2.0.43

# MQTT client for real-time communication
paho-mqtt==2.1.0

# HTTP requests for API calls
requests==2.32.5

# Google AI/Gemini integration
google-generativeai==0.8.5

# Machine Learning and AI dependencies (needs to be installed)
transformers==4.36.0
torch==2.1.0
accelerate==0.25.0

# Core dependencies already in your venv
annotated-types==0.7.0
anyio==4.11.0
click==8.3.0
colorama==0.4.6
h11==0.16.0
idna==3.10
sniffio==1.3.1
typing-extensions==4.15.0
typing-inspection==0.4.2

# Development and testing (needs to be installed)
pytest==7.4.3
pytest-asyncio==0.21.1

# Optional: For better performance
python-multipart==0.0.6

# Note: The following are built-in Python modules and don't need installation:
# sqlite3, json, uuid, datetime, typing, threading, logging, re, os, sys, 
# subprocess, socket, time, random, dataclasses, enum, pathlib
